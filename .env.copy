APP_NAME="Chaox Auth"
FLASK_PORT=5000
SECRET_KEY="secret_key"
# Para generar tu ENCRYPTION_KEY, ejecuta en python: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())
ENCRYPTION_KEY="y-S1_vLp_R8v_f_08pS_tV-F-7_H8M_j_H8v_f_08pS="
DB_NAME=".db"
LOCK_TIME=60

# --- OPCIONALES (Valores por defecto en el código) ---
# CAMERA_INDEX=0
# MAX_ATTEMPTS=3
# CAMERA_WIDTH=640
# CAMERA_HEIGHT=480
# JPEG_QUALITY=35
# INFERENCE_INTERVAL=0.15
